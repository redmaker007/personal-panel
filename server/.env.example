# 複製成 server/.env（已 gitignore），填上自己的值。
# npm scripts 用 --env-file-if-exists=.env 載入；沒有管理員密碼時關閉後台登入。

# 後台登入密碼。請填入自己的強密碼。
ADMIN_PASSWORD=

# 伺服器埠號，預設 4000
# PORT=4000

# SQLite 檔位置，預設 server/data/panel.db
# DB_PATH=./data/panel.db
