# 複製成 server/.env（已 gitignore），填上自己的值。
# npm scripts 用 --env-file-if-exists=.env 載入，不存在就用程式裡的預設值。

# 後台登入密碼。不設就是 "changeme"。
ADMIN_PASSWORD=changeme

# 伺服器埠號，預設 4000
# PORT=4000

# SQLite 檔位置，預設 server/data/panel.db
# DB_PATH=./data/panel.db
