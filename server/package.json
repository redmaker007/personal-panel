{
  "name": "personal-panel-admin",
  "version": "0.1.0",
  "private": true,
  "description": "個人屬性面板 — 後台（題目編輯 + 選項數據）",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "start": "node --disable-warning=ExperimentalWarning --env-file-if-exists=.env src/index.js",
    "dev": "node --disable-warning=ExperimentalWarning --env-file-if-exists=.env --watch src/index.js",
    "import": "node --disable-warning=ExperimentalWarning --env-file-if-exists=.env src/import-questions.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
